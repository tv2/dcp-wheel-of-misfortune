# How to
1. build the docker image locally
docker build -t <some registry>/some-image:<some tag> .

(I've used my private manedev-repository on dockerhub)


2. push the docker image to some registry
docker push <some registry>/some-image:<some tag>

